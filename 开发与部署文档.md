# Windows 全终端现代化与美化套件 —— 架构开发与部署全景指南

> 本文档面向系统管理员、开发者与终端发烧友，深入剖析本项目的系统架构设计、三级包管理容错降级机制、PowerShell 7 每日随机主题算法、全自动前置快照回退体系以及 Windows 平台编码避坑经验。

---

## 目录
1. [项目设计哲学与核心架构](#1-项目设计哲学与核心架构)
2. [全终端支持与配置资产拓扑](#2-全终端支持与配置资产拓扑)
3. [高稳定性包管理与环境准备机制 (容错审计)](#3-高稳定性包管理与环境准备机制-容错审计)
4. [PowerShell 7 终端美化与“每日随机主题”实现](#4-powershell-7-终端美化与每日随机主题实现)
5. [Fastfetch 动漫横幅与终端就绪卡片体系](#5-fastfetch-动漫横幅与终端就绪卡片体系)
6. [完整部署与安装实操指南](#6-完整部署与安装实操指南)
7. [无损备份与一键原子回退机制](#7-无损备份与一键原子回退机制)
8. [编码规范与 Windows 避坑经验 (CRITICAL)](#8-编码规范与-windows-避坑经验-critical)
9. [自动化验证与回归测试体系](#9-自动化验证与回归测试体系)
10. [常见问题排查与 FAQ](#10-常见问题排查与-faq)
11. [IDE 与编辑器集成方案 (VS Code / IntelliJ IDEA)](#11-ide-与编辑器集成方案-vs-code--intellij-idea)

---

## 1. 项目设计哲学与核心架构

在 Windows 环境下，终端美化与开发工具链常常面临以下几大痛点：
1. **安装成功率低**：依赖单一包管理器（如只有 Scoop 或只有 Choco），在不同网络环境（代理、公司内网、国内源超时）下经常报错中断；
2. **缺乏状态保护**：安装或修改 `$PROFILE` 时直接覆盖现有配置，导致用户原本精心编写的别名、环境变量丢失，无法复原；
3. **冷启动卡顿**：在 Profile 中进行网络检测或大模块无序引入，导致终端打开需等待 2-5 秒；
4. **编码兼容性灾难**：Windows PowerShell 5.1 在默认代码页 936 (GBK) 下，处理无 BOM 的 UTF-8 脚本时会发生引号解析错位与语法崩溃。

本项目秉承以下设计原则：
- **高可用与极高成功率 (High Availability & Fault Tolerance)**：自愈式三级包管理降级（Choco ➜ WinGet ➜ Scoop），前置 NuGet 与 PSGallery 信任，就绪状态毫秒级跳过；
- **启动零网络请求与高性能 (Zero-Network Startup & High Performance)**：所有主题与插件均本地化，Profile 严格杜绝网络请求；
- **全生命周期安全回退 (Zero-Risk Rollback)**：任何操作前强制创建 SHA-256 镜像备份，配备 `-WhatIf` 预演与双向回退；
- **双解析器完全平权 (Dual-Engine Equivalence)**：一套源码同时在 `PowerShell 7 (Core)` 与 `Windows PowerShell 5.1 (Framework)` 下 100% 通过语法分析与行为验证。

```mermaid
flowchart TD
    A[用户运行安装/部署脚本] --> B[创建系统前置快照 SHA-256 镜像]
    B --> C[前置检查与包管理环境审计]
    C --> D{工具链是否已就绪?}
    D -- 是 --> E[毫秒级跳过, 零网络浪费]
    D -- 否 --> F[第一梯队: Chocolatey 尝试安装, 失败自动重试2次]
    F -- 失败/不可用 --> G[第二梯队: WinGet 自动源重置与配置, 执行安装]
    G -- 失败/不可用 --> H[第三梯队: Scoop 仓库与 Shims 保底安装]
    E --> I[分发本地内聚配置与 Profile]
    F -- 成功 --> I
    G -- 成功 --> I
    H -- 成功 --> I
    I --> J[本地 120+ Oh-My-Posh 主题与 Starship 本地化部署]
    J --> K[Fastfetch 动漫 ASCII Art 与就绪卡片关联]
    K --> L[验证文件完整性并输出部署报告]
```

---

## 2. 全终端支持与配置资产拓扑

本项目实现了对 Windows 体系内 5 大主流命令行运行时的完整美化与生产力加速：

| 终端环境 | 运行进程 | 配置文件部署目标 | 核心美化/增强技术 | 特色能力 |
| :--- | :--- | :--- | :--- | :--- |
| **PowerShell 7** | `pwsh.exe` | `~/Documents/PowerShell/Microsoft.PowerShell_profile.ps1` | Oh-My-Posh (120+主题) / Starship + PSReadLine + Terminal-Icons | **默认每日随机主题**、Yazi 目录穿梭、预测补全、全 Unix 别名 |
| **WinPS 5.1** | `powershell.exe` | `~/Documents/WindowsPowerShell/Microsoft.PowerShell_profile.ps1` | 专为 5.1 调优的极速 Starship + UTF-8 运行时修复 | 强制 UTF-8 根治 GBK 乱码、毫秒级启动、全 Unix 别名 |
| **CMD** | `cmd.exe` | `~/AppData/Local/clink/` + 注册表 AutoRun | Clink Lua + Starship 赛博朋克渐变提示符 | 实时语法着色、历史搜索、Unix 命令映射（`ll`, `cat` 等） |
| **NuShell** | `nu.exe` | `~/AppData/Roaming/nushell/config.nu` + `env.nu` | Starship 结构化渲染 + Zoxide 快跳集成 | 结构化数据流水线、现代别名、Fastfetch 专属卡片 |
| **MSYS2** | `bash.exe` | `C:/msys64/home/<User>/.bashrc` | Starship Linux 提示符 + PATH 继承 | 继承 Windows 原生 PATH，直接调用宿主编译器与工具 |
| **Windows Terminal** | `wt.exe` | `~/AppData/Local/Packages/.../LocalState/settings.json` | 亚克力磨砂透明、Catppuccin 配色、JetBrainsMono 字体 | 深度定制配色、标签页快速呼出、快捷键增强 |

---

## 3. 高稳定性包管理与环境准备机制 (容错审计)

针对用户提出的“**安装稳定成功率要高**”的核心诉求，项目在底层通用库 [`scripts/TerminalSetupCommon.ps1`](file:///c:/XMWJJ/powershelldome/scripts/TerminalSetupCommon.ps1) 中实施了严苛的稳态控制机制：

### 3.1 前置解决 PowerShell Gallery 与 NuGet 挂起问题
在 Windows 纯净系统或未深度配置的环境下，通过 `Install-Module` 安装插件（如 `PSReadLine`, `Terminal-Icons`）时，PowerShell 往往会弹出：
> *"NuGet provider is required to continue"* 或 *"You are installing the modules from an untrusted repository."*

若无人值守或在非交互式环境中运行，脚本将死锁挂起或直接抛出异常中断。为此，我们在 `Install-PSModulesIfMissing` 中固化了前置自愈逻辑：
```powershell
# 1. 强制启用安全传输协议 TLS 1.2
[Net.ServicePointManager]::SecurityProtocol = [Net.ServicePointManager]::SecurityProtocol -bor [Net.SecurityProtocolType]::Tls12

# 2. 自动化静默安装并导入最新版 NuGet PackageProvider
$nuget = Get-PackageProvider -Name NuGet -ListAvailable -ErrorAction SilentlyContinue | Where-Object { $_.Version -ge [Version]'2.8.5.201' }
if (-not $nuget) {
    Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force -Scope CurrentUser -Confirm:$false | Out-Null
}

# 3. 将官方 PSGallery 设为 Trusted，杜绝提示交互挂起
Set-PSRepository -Name 'PSGallery' -InstallationPolicy Trusted -ErrorAction SilentlyContinue
```

### 3.2 智能包管理三级容错与重试降级链 (Choco ➜ WinGet ➜ Scoop)
对于外部 CLI 软件（如 `starship`, `fastfetch`, `eza`, `bat`, `ripgrep`, `zoxide`, `yazi`）：
1. **就绪快速跳过 (Fast-Path Check)**：
   每次安装前优先执行 `Get-Command $tool -ErrorAction SilentlyContinue`，若已存在于 PATH 中，立刻返回成功，整体脚本耗时从数分钟降至秒级；
2. **第一梯队：Chocolatey 稳态安装与自动重试**：
   若检测到系统有 `choco`，执行安装，并捕获任何网络或锁文件错误。若失败，**内置指数回避机制并自动重试最多 2 次**；
3. **第二梯队：WinGet 自动修复与备用接管**：
   若 Choco 不可用或重试 2 次仍然失败，自动平滑切换到现代 Windows 标配的 WinGet。在执行安装前，自动运行：
   `winget source reset --force` 及 `winget source update`
   重置可能损坏或超时的本地源缓存，随后使用精确 `--id` 静默安装；
4. **第三梯队：Scoop 终极兜底 (跨版本保底)**：
   针对 Windows 8.1 等无法运行 WinGet 的遗留系统，自动调用 Scoop 软件源并配置相关 shims。

---

## 4. PowerShell 7 终端美化与“每日随机主题”实现

### 4.1 架构原理
以往终端美化要么固定单一主题（容易视觉疲劳），要么必须每次在线从 GitHub 下载主题（受到网络波动与代理影响极大，甚至拖慢启动 2-5 秒）。

本项目采用了**本地离线 120+ 官方全量主题库 + 智能抽取算法 + 异常安全降级**的组合架构：
- 在安装阶段，`Ensure-OhMyPoshThemes` 会扫描本地 Scoop、WinGet、Program Files 及 `$env:POSH_THEMES_PATH`，将 120+ 官方精美主题同步至用户统一目录 `~/.posh-themes/`；
- 在用户 Profile 启动时，由本地纯内存算法执行随机选择，整个过程耗时 **< 15ms**，零外部网络依赖。

### 4.2 智能随机与异常降级算法实现
在 [`Microsoft.PowerShell_profile.ps1`](file:///c:/XMWJJ/powershelldome/Microsoft.PowerShell_profile.ps1) 中核心逻辑如下：

```powershell
if ($themeMode -eq 'random' -and (Test-Path -LiteralPath $themesDir -PathType Container)) {
    # 扫描并过滤所有可用的 json/omp.json 主题文件
    $allThemes = @(Get-ChildItem -LiteralPath $themesDir -File | Where-Object { $_.Extension -in '.json', '.omp' -or $_.Name -like '*.omp.json' })
    if ($allThemes.Count -gt 0) {
        # 智能随机抽取
        $chosen = $allThemes | Get-Random
        $chosenTheme = $chosen.FullName
        $cleanThemeName = $chosen.BaseName -replace '\.omp$', ''
        
        # 优雅提示当前抽取到的主题名
        Write-Host "✨ 今日随机主题: " -NoNewline -ForegroundColor DarkGray
        Write-Host "$cleanThemeName" -ForegroundColor Cyan
    }
}

# 加载主题，配备双层异常保护：若选中的主题格式异常，自动无缝回退至内置 Catppuccin Mocha
try {
    oh-my-posh init pwsh --config $chosenTheme | Invoke-Expression
} catch {
    Write-Warning "Oh-My-Posh 主题加载异常，降级回退至 Catppuccin Mocha: $_"
    oh-my-posh init pwsh --config (Join-Path $PSScriptRoot 'starship\catppuccin_mocha.omp.json') | Invoke-Expression
}
```

### 4.3 自由切换主题模式
用户可以在任何时候通过环境变量或运行命令自由调整主题模式：
- **切换回固定经典主题**：
  ```powershell
  $env:POWERSHELL_THEME_MODE = 'fixed'
  ```
- **切换回 Starship 赛博朋克极速主题**：
  ```powershell
  $env:POWERSHELL_THEME_MODE = 'starship'
  ```
- **随时开启每日随机主题**：
  ```powershell
  $env:POWERSHELL_THEME_MODE = 'random'
  ```
  *(注：若需永久生效，在安装脚本 `Install-PowerShell7.ps1` 提示时选择相应模式，或在 `$PROFILE` 顶部声明即可。)*

---

## 5. Fastfetch 动漫横幅与终端就绪卡片体系

### 5.1 资产自包含与内聚设计
传统 Fastfetch 美化往往需要手动寻找字符画并将配置文件放置在复杂路径。本项目在仓库根目录直接内置了完整生产力资产：
- [`fastfetch/ascii.txt`](file:///c:/XMWJJ/powershelldome/fastfetch/ascii.txt)：精制二次元/动漫高密度点阵字符画；
- [`fastfetch/config.jsonc`](file:///c:/XMWJJ/powershelldome/fastfetch/config.jsonc)：基于 Catppuccin Mocha 配色方案的硬件与软件状态采集器。

### 5.2 绝对路径自动化适配与跨终端复用
在执行任何终端安装脚本时，底层库会自动检测并将横幅与配置复制到 `%USERPROFILE%/.config/fastfetch/`，并将 `config.jsonc` 内部的 ASCII 图标路径动态替换为本机的物理绝对路径（采用标准正斜杠 `/` 规避 JSON 转义错误）。

### 5.3 终端功能就绪卡片（真实加载检测与双列紧凑仪表盘）
当新开任何一个美化后的终端时，控制台不仅展示硬件与操作系统信息，还会通过后台真实状态探测，输出**双列对齐紧凑仪表盘**：
- **严格真实检测**：逐项检测命令可执行性、模块导入与按键绑定，成功显示绿色 `✓`；
- **异常明确告警**：若某个外部工具未安装或功能降级，自动在告警栏清晰标黄输出 `✗ <功能> 未就绪: 缺少 <工具名>, 建议运行: scoop install <工具名>`，绝不虚假显示成功；
- **自适应宽度**：终端宽度小于 82 列时自动切换为单列紧凑流，避免边框换行错位；
- **快捷键速查条**：底部附带常用高频快捷键速查。

```text
┌─ 🚀 终端现代化生产力就绪看板 ────────────────────────────────────────────────────────┐
│ ✓ yazi 目录穿梭 (y [path])             ✓ fd 极速索引引擎 (FZF加速)            │
│ ✓ bat+eza 画中画实时预览               ✓ fzf 模糊搜索 (Ctrl+R/F/Alt+Z)        │
│ ✓ eza 现代文件列表 (ll/la/lt/lg)       ✓ lazydocker 容器管家 (lzd)            │
│ ✓ Neovim 默认编辑器 (v/fv)             ✓ zoxide 智能目录快跳 (z/zi)           │
│ ✓ fif 全文代码检索 (fif <词>)          ✓ fastfetch 动漫硬件看板               │
├───────────────────────────────────────────────────────────────────────────────────────┤
│ ⚡ 快捷操作: [Ctrl+R] 历史搜 | [Alt+Z/zi] 目录跳 | [Ctrl+F] 文件填 | [fv] 模糊编辑 | [fif] 全文搜 │
└───────────────────────────────────────────────────────────────────────────────────────┘
```

### 5.4 生产力增强指令集 (A+B 方案)
- **`fv` (Fuzzy View & Edit)**：交互式模糊查找当前目录文件（右侧以 `bat` 实时语法高亮预览），回车直接使用 Neovim 打开；
- **`fif <关键词>` (Find In Files)**：联动 `ripgrep` + `fzf` + `bat` + `nvim`，实现毫秒级全文秒搜，回车光标直达 Neovim 目标代码行；
- **`v <文件/目录>`**：单字母极速呼出 Neovim；
- **`lt` / `lt3`**：调用 `eza` 分别以 2 层和 3 层树状图展示当前目录；
- **`lg`**：调用 `eza --long --git`，文件列表直接显示 Git 变更与暂存状态；
- **`..` / `...` / `....`**：极速上溯父级目录；
- **FZF 画中画预览**：集成 Catppuccin Mocha 终端配色、圆角边框、文件实时代码高亮与目录树动态预览。

---

## 6. 完整部署与安装实操指南

### 6.1 全新机器/重装系统一键总装（推荐）
在具备管理员权限或普通用户权限的 PowerShell 中执行：
```powershell
# 1. 克隆代码仓库
git clone https://github.com/vicky-clair/configuration_of_Terminal_and_PowerShell_for_Xia_Rui.git
cd configuration_of_Terminal_and_PowerShell_for_Xia_Rui

# 2. 一键总装 5 大全套终端环境（PS7 + WinPS 5.1 + CMD + NuShell + MSYS2）
pwsh -File .\Install-All.ps1 -All
# 若系统中尚未安装 PowerShell 7，可先用 Windows 原生 PowerShell 执行：
powershell.exe -ExecutionPolicy Bypass -File .\Install-All.ps1 -All
```

### 6.2 独立终端按需安装与定制
| 目标终端 | 安装命令 | 默认特性与配置项 |
| :--- | :--- | :--- |
| **PowerShell 7** | `pwsh -File .\Install-PowerShell7.ps1` | **默认配置每日随机主题**，亦可自由选择固定主题或 Starship |
| **WinPS 5.1** | `powershell.exe -ExecutionPolicy Bypass -File .\Install-WinPowerShell51.ps1` | 极速原生 Starship，永久修复 UTF-8 代码页 |
| **CMD** | `pwsh -File .\Install-Cmd.ps1` | 安装 Clink、Starship、注册表自动挂载 |
| **NuShell** | `pwsh -File .\Install-NuShell.ps1` | 结构化脚本配置、UTF-8 中文环境、Starship 集成 |
| **MSYS2** | `pwsh -File .\Install-MSYS2.ps1` | 自动配置环境继承、原生工具互通、Bash 提示符 |

---

## 7. 无损备份与一键原子回退机制

### 7.1 备份快照的生成与清单结构
在对终端配置进行任何实质写入前，底层调度器会扫描所有受管目标，读取原始字节流并计算 SHA-256 哈希，存放在 `backups/deployment-<Timestamp>/` 下。

清单文件 `deployment.json` 样例如下：
```json
{
  "CreatedAt": "2026-09-07T08:32:32.1234567+08:00",
  "Files": [
    {
      "Target": "C:\\Users\\User\\Documents\\PowerShell\\Microsoft.PowerShell_profile.ps1",
      "Before": "PowerShell7-profile.before.ps1",
      "After": "PowerShell7-profile.after.ps1",
      "BeforeSHA256": "4A1B...",
      "AfterSHA256": "8F2C..."
    }
  ]
}
```

### 7.2 安全回退流程与两阶段救援快照
当运行 `Restore-All.ps1` 时：
1. **预演模式 (`-WhatIf`)**：
   ```powershell
   pwsh -File .\Restore-All.ps1 -WhatIf
   ```
   仅校验快照哈希与目标状态，展示即将发生的操作，绝不触碰磁盘。
2. **两阶段救援 (`pre-rollback`) 保护**：
   在回退执行的一瞬间，脚本会先将**当前正在运行的配置**原样封存至 `snapshots/pre-rollback-<Timestamp>/`。即使之后用户改变主意想要重新应用之前的个性化修改，数据也永不丢失。
3. **原子替换与文件锁重试**：
   针对 Windows 文件句柄由于安全杀毒软件扫描短暂占用问题，内置 `Set-TargetBytesSafely`（10 次带间隔自愈重试），确保回退过程绝对顺畅。

---

## 8. 编码规范与 Windows 避坑经验 (CRITICAL)

在跨 PowerShell 7 与 Windows PowerShell 5.1 进行脚本开发时，存在几条由 Windows 底层历史包袱引起的“隐形天坑”。本项目通过严谨的编码规范全面规避：

### 8.1 UTF-8 with BOM 规则 (PowerShell 脚本铁律)
- **现象**：在 Windows PowerShell 5.1（代码页 936 / GBK）环境下，若 `.ps1` 文件保存为不带 BOM 的普通 UTF-8，当脚本中包含任何中文字符（如注释、提示文本）时，Windows PowerShell 会以系统默认代码页（GBK）去解码多字节的 UTF-8 序列。在特定汉字字节组合后，双引号 `"` 会被错误吞噬，导致触发致命语法错误：
  > `ParseException: 表达式或语句中包含意外的标记“}”。`
- **规约**：本项目**除备份只读快照外，所有的 `.ps1` 脚本文件一律强制使用 UTF-8 with BOM (`EF BB BF`) 存储**。在写入脚本时必须使用：
  ```powershell
  [System.IO.File]::WriteAllText($path, $content, (New-Object System.Text.UTF8Encoding $true))
  ```

### 8.2 UTF-8 without BOM 规则 (现代 CLI 工具铁律)
- **NuShell (`.nu`)**：NuShell 语法解析器不允许首字符出现 BOM 字节，否则会将 `\uFEFF#` 识别为未知外部命令导致报错：`Command '#' not found`。
- **Fastfetch (`config.jsonc`)**：官方 JSONC 解析器严格遵循 JSON 标准，若存在 BOM 会抛出：`UTF-8 byte order mark (BOM) is not supported`。
- **规约**：项目中的 `fastfetch/*.jsonc` 和 `nushell/*.nu` 配置文件**一律严禁包含 BOM**。

### 8.3 批处理换行符规则 (CMD autorun.cmd)
- `cmd/autorun.cmd` 必须使用标准 Windows 回车换行符 **CRLF (`\r\n`)**，避免在老版本 `cmd.exe` 批量解析批处理标签时发生语法截断。

---

## 9. 自动化验证与回归测试体系

为了保证本套美化与配置资产在任何 Windows 系统上均可稳定运行，项目配备了自动化测试集：

### 9.1 静态语法与配置自检 (`tests/Verify-Configuration.ps1`)
```powershell
# 在 PowerShell 7 中运行：
pwsh -NoProfile -ExecutionPolicy Bypass -File .\tests\Verify-Configuration.ps1

# 在 Windows PowerShell 5.1 中运行：
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Verify-Configuration.ps1
```
测试项覆盖：
- `settings.json` 的 GUID 唯一性与 DefaultProfile 有效性；
- 所有 `.ps1` 脚本通过 `[System.Management.Automation.Language.Parser]` 抽象语法树校验，确保 0 ParseErrors；
- Fastfetch 资产、CMD Clink 脚本的完整存在性；
- Profile 在极简模式下的无害静默加载、别名覆盖校验（`dir`, `ls`, `cat` 保护）与 Git 参数前向透传测试。

### 9.2 沙箱隔离部署与回退回归测试 (`tests/Verify-Deployment.ps1`)
```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\tests\Verify-Deployment.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tests\Verify-Deployment.ps1
```
测试项覆盖：
- 隔离虚拟沙箱环境中的原子 Apply 部署；
- 部署后 SHA-256 镜像的一致性断言；
- `-WhatIf` 预演的只读性保护断言；
- 现场修改文件触发冲突时的拒绝覆盖（Fail-Safe）防护；
- 真实回退与两阶段救援快照（`pre-rollback`）的完整性断言。

---

## 10. 常见问题排查与 FAQ

### Q1: 启动 PowerShell 提示“因为在此系统上禁止运行脚本...”？
**解答**：Windows 默认执行策略限制。只需在当前用户作用域内放行即可（无需全局管理员）：
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
```

### Q2: 图标显示为乱码方块或问号？
**解答**：这是由于当前控制台未启用 Nerd Font 图标字体。
1. 请下载并安装 [JetBrainsMono Nerd Font](https://www.nerdfonts.com/font-downloads)；
2. 在 Windows Terminal 设置中，进入对应 Profile ➜ 外观 ➜ 字体，选择 `JetBrainsMono Nerd Font` 即可完美呈现全部图标。

### Q3: 为什么今日随机抽到的主题我想固定下来？
**解答**：若您喜欢某天抽到的主题（例如 `amro` 或 `tokyonight_storm`）：
1. 记下控制台顶部打印的主题名称；
2. 运行 `pwsh -File .\Install-PowerShell7.ps1`，输入 `1` 选择固定主题并填入该名称；或者直接在 `$PROFILE` 中将主题路径指定为该文件即可。

### Q4: 怎样完全卸载所有终端美化并恢复原状？
**解答**：直接运行本项目根目录下的回退脚本：
```powershell
pwsh -File .\Restore-All.ps1
```
脚本将基于部署前的 SHA-256 快照，一键将注册表、Profile、Windows Terminal 设置全量精确复原。

---

## 11. IDE 与编辑器集成方案 (VS Code / IntelliJ IDEA)

本项目所配置的终端不仅能在独立 Windows Terminal 窗口中运行，更专为现代开发者的主力 IDE（VS Code、IntelliJ IDEA、PyCharm、WebStorm 等）进行了专项适配。

### 11.1 核心配置原则
- **独立字体配置**：IDE 内置终端必须明确选择带有 `Nerd Font`、`NF` 或 `NFP` 后缀的字体（如 `JetBrainsMono NFP`），不可直接继承普通编辑器的代码字体；
- **编码强制 UTF-8**：在终端启动时确保代码页为 `65001`，彻底杜绝 Maven、Gradle、Git 提交记录中的中文乱码；
- **极简横幅适配**：若 IDE 底部终端窗口高度较小，可通过在 IDE 环境变量中设置 `POWERSHELL_PROFILE_BANNER=0` 隐藏大型 Fastfetch 字符画，实现极简秒开。

详细的一键 JSON 配置、多 Shell Profile 配置与图形界面操作步骤，请参阅：
👉 **[IDE 与开发工具集成指南 (IDE与开发工具集成指南.md)](IDE与开发工具集成指南.md)**。

