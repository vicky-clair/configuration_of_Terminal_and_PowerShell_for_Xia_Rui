# 本机终端部署与回退

部署快照：`backups/deployment-20260907-060507/`。所有原始备份均按字节复制，并记录 SHA-256 校验值。此快照与早先的 `backups/original/` 不同，是应用前从本机实时配置生成的。

## 修改范围

仅更新两个现有文件：

1. `C:\Users\xasr2\Documents\PowerShell\Microsoft.PowerShell_profile.ps1`
2. `C:\Users\xasr2\AppData\Local\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState\settings.json`

PowerShell 配置直接复制为独立文件，不依赖项目目录才能启动。保留实时 Profile 中的 `mkcd`、`Find-LargeFiles`、编辑/重载配置、系统信息、网络测试、环境自检、带时间戳日志和 `gps` 等函数。原 `gp` 与内置 `Get-ItemProperty` 别名冲突，拉取命令改用 `gpull`；原 `gl` 与 `Get-Location` 冲突，日志命令使用 `glog`。

不修改 Windows PowerShell 5.1 的 Starship Profile、Terminal Preview、字体文件、壁纸、NuShell 配置、Scoop 工具、系统 PATH、注册表或执行策略。没有安装或升级软件。两个未修改的 Profile/Terminal Preview 配置也做了只读快照，以便复核。

## 如何生效

新开 PowerShell 7 标签页即可加载新脚本。已打开的 PowerShell 会话仍保留原函数和别名，建议新开标签页，避免在旧会话里反复加载。Terminal 会读取设置文件；若外观未刷新，保存当前工作后重新打开 Terminal。

## 一键回退

保存当前终端工作，然后在 PowerShell 里运行：

```powershell
pwsh -NoProfile -File 'C:\XMWJJ\powershelldome\Restore-TerminalConfiguration.ps1'
```

只预览回退目标，不做修改：

```powershell
pwsh -NoProfile -File 'C:\XMWJJ\powershelldome\Restore-TerminalConfiguration.ps1' -WhatIf
```

回退会先将当时的两个配置保存到快照目录内的 `pre-rollback-时间戳/`，再恢复部署前的原始字节，并核对 SHA-256。即使部署后又编辑了配置，也可以从这份二次备份找回。回退后新开 PowerShell 标签页。

请保留本项目及整个部署快照目录，以便执行上述回退。日常终端运行不依赖这些备份文件。

## 无法执行脚本时手动回退

若执行策略阻止 `.ps1`，无需改变策略。在 `pwsh -NoProfile` 打开的窗口中逐行执行以下命令；路径均固定，只涉及两个终端配置。先备份当前状态，再覆盖：

```powershell
$restoreSource = 'C:\XMWJJ\powershelldome\backups\deployment-20260907-060507'
$rescuePath = Join-Path $restoreSource ('manual-pre-rollback-' + (Get-Date -Format 'yyyyMMdd-HHmmss-fff'))
New-Item -ItemType Directory -Path $rescuePath -ErrorAction Stop | Out-Null
$psTarget = 'C:\Users\xasr2\Documents\PowerShell\Microsoft.PowerShell_profile.ps1'
$wtTarget = 'C:\Users\xasr2\AppData\Local\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState\settings.json'
Copy-Item -LiteralPath $psTarget -Destination (Join-Path $rescuePath 'PowerShell7-profile.ps1') -ErrorAction Stop
Copy-Item -LiteralPath $wtTarget -Destination (Join-Path $rescuePath 'Terminal-stable.json') -ErrorAction Stop
Copy-Item -LiteralPath (Join-Path $restoreSource 'PowerShell7-profile.before.ps1') -Destination $psTarget -ErrorAction Stop
Copy-Item -LiteralPath (Join-Path $restoreSource 'Terminal-stable.before.json') -Destination $wtTarget -ErrorAction Stop
```

## 验证与记录

`snapshot.json` 记录四个原始文件的来源及校验值；`deployment.json` 限定两个目标及部署前后校验值；成功操作会生成 `last-apply.json` 或 `last-rollback.json`。

部署脚本在写入前检查备份完整性、语法及实时文件是否发生变化。若发生中途写入失败，会尝试恢复已修改文件。回归测试在项目内的隔离目录执行完整应用和回退，不用真实配置做回退试验。

界面渲染与需要真实输入的交互快捷键仍需在新开的 Terminal 标签页中体验确认。
