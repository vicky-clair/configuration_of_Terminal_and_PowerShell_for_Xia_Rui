# 本机终端部署与回退手册

本项目建立了完整的分级备份、版本可追溯快照与安全回退体系。在对系统进行任何实质修改前，所有相关配置均已按字节复制并生成 SHA-256 校验和。

> [!IMPORTANT]
> **安装前须知与手动备份建议**：由于各用户电脑的操作系统分支、已装软件环境、PowerShell 执行策略与权限管控存在差异，脚本无法保证完全契合所有主机。在运行任何安装或部署脚本前，强烈建议手动复制备份已有的个人 `$PROFILE`、Windows Terminal 自定义设置及重要数据。

---

## 📦 备份历史与快照存档

1. **系统全量审计快照**：
   - 路径：[`backups/system-audit-20260907-082932/`](file:///c:/XMWJJ/powershelldome/backups/system-audit-20260907-082932/)
   - 说明：包含了本次审计时系统的所有原始文件（CMD Clink 脚本与历史、PowerShell 7 与 5.1 Profile、Windows Terminal 稳定版与预览版设置、Fastfetch 配置与 ASCII 图标、Starship 配置、NuShell 配置及注册表导出）。
2. **当前生效部署快照**：
   - 路径：由项目根目录 `.deployment-path` 指向，不应硬编码历史目录。
   - 说明：新版部署保存修改前的文件及 `manifest.json`；历史 `deployment.json` 快照仍由仓库中的旧版管理器读取。
3. **初始优化快照**：
   - `backups/deployment-20260907-060507/` 与 `backups/original/`。

---

## 🚀 部署操作指南

在 PowerShell（推荐 PowerShell 7）中运行：

### 1. 预演部署（不修改任何文件）
```powershell
pwsh -NoProfile -File 'C:\XMWJJ\powershelldome\Deploy-TerminalConfiguration.ps1' -WhatIf
```

### 2. 执行完整部署
```powershell
pwsh -NoProfile -File 'C:\XMWJJ\powershelldome\Deploy-TerminalConfiguration.ps1'
```
执行内容：
1. 自动执行 `tests/Verify-Configuration.ps1` 静态与语法校验；
2. 自动捕获当前系统的实时字节快照；
3. 将增强版 `Microsoft.PowerShell_profile.ps1` 写入 `$PROFILE.CurrentUserCurrentHost`；
4. 将 `settings.json` 写入 Windows Terminal 稳定版配置；
5. 部署 `cmd/autorun.cmd` 与 Clink 脚本至当前用户配置目录，并设置当前用户注册表 AutoRun。

### 3. 五个独立一键安装脚本（支持全新机器从零配置与包管理容错降级）
当在新设备或重装系统后（支持 Windows 11、Windows 10 及 Windows 8.1），可使用以下脚本一键安装包管理仓库、工具链与美化：

- **PowerShell 7 安装脚本**：[`Install-PowerShell7.ps1`](file:///c:/XMWJJ/powershelldome/Install-PowerShell7.ps1)
  ```powershell
  pwsh -File .\Install-PowerShell7.ps1
  ```
  自动配置 Scoop 仓库与工具，交互式提示选择【固定主题 (Catppuccin Mocha)】或【每日随机主题】或【Starship 赛博朋克主题】。
- **Windows PowerShell 5.1 安装脚本**：[`Install-WinPowerShell51.ps1`](file:///c:/XMWJJ/powershelldome/Install-WinPowerShell51.ps1)
  ```powershell
  powershell.exe -File .\Install-WinPowerShell51.ps1
  ```
  内置 UTF-8 编码修复，固定加载极速 Starship 原生提示符，秒开无延迟。
- **CMD 命令提示符安装脚本**：[`Install-Cmd.ps1`](file:///c:/XMWJJ/powershelldome/Install-Cmd.ps1)
  ```powershell
  pwsh -File .\Install-Cmd.ps1
  # 或卸载：pwsh -File .\Install-Cmd.ps1 -Uninstall
  ```
  自动安装 Clink、Starship、Eza、Bat、Ripgrep，固定赛博朋克霓虹主题与完整 Unix 别名。
- **NuShell 安装配置脚本**：[`Install-NuShell.ps1`](file:///c:/XMWJJ/powershelldome/Install-NuShell.ps1)
  ```powershell
  pwsh -File .\Install-NuShell.ps1
  ```
  自动挂载 Starship 赛博朋克提示符与 Zoxide 目录快跳，配置 Fastfetch 启动横幅与 Unix/Git 常用别名，自动配置 `env.nu` UTF-8 中文环境。
- **MSYS2 开发终端安装配置脚本**：[`Install-MSYS2.ps1`](file:///c:/XMWJJ/powershelldome/Install-MSYS2.ps1)
  ```powershell
  # 默认路径安装与配置：
  pwsh -File .\Install-MSYS2.ps1
  # 若 MSYS2 位于自定义路径，传入参数：
  # pwsh -File .\Install-MSYS2.ps1 -Msys2InstallPath "D:\Tools\msys64"
  ```
  自动配置 `MSYS2_PATH_TYPE=inherit` 继承 Windows 本机环境变量，可在 MSYS2 中直接调用 Windows 原生安装的工具；配置 `~/.bashrc` 强制 UTF-8、Starship 提示符与常用别名；**自动检查并向 Windows Terminal 注册 UCRT64 入口（若未注册）**。
- **全终端集成总装脚本**：[`Install-All.ps1`](file:///c:/XMWJJ/powershelldome/Install-All.ps1)
  ```powershell
  pwsh -File .\Install-All.ps1 -All
  ```

#### 包管理器容错降级与自愈机制（Choco ➜ WinGet ➜ Scoop ➜ CDN 直连）
所有安装脚本在安装外部软件（NuShell、MSYS2、Starship、Fastfetch、Eza、Bat、Zoxide、JetBrainsMono 等）时，严格遵循以下降级链条与自愈机制：
1. **优先检测本机就绪状态**：已就绪直接跳过，零网络浪费；
2. **第一梯队 (Chocolatey)**：若存在 `choco`，执行安装；若失败最多**自动重试 2 次**；
3. **第二梯队 (WinGet 自动配置与安装)**：若无 `choco` 或 2 次尝试均失败：
   - 自动检测并配置/重置 WinGet 仓库软件源（`winget source reset --force` / `winget source update`）；
   - 执行 `winget install --id ...` 静默安装；
4. **第三梯队 (Scoop 终极保底与镜像自愈)**：在 WinGet 无法使用或包缺失时自动启用 Scoop：
   - **国内镜像自愈**：添加 bucket 时若遇 GitHub 网络重置，自动无缝切换至 `gh-proxy.com` 或 Gitee 镜像仓库；
   - **损坏仓库自动修复**：若 bucket 文件夹为空或 git 克隆中断损坏，自动识别并清理重新拉取；
   - **空状态容错**：精准处理 `scoop list` 空应用时退出码为 1 及信息流 6 的特殊逻辑，杜绝误报失败；
5. **第四梯队 (字体 CDN 直链下载保底)**：若 Scoop 字体仓库仍无法获取 `JetBrainsMono-NF`，自动启动备用高速 CDN 下载解压，写入系统字体目录并注册当前用户字体注册表，彻底解决 Windows Terminal 弹窗找不到字体问题；
6. **环境执行策略与安全锁定自愈**：
   - 自动将当前用户脚本策略安全升级为 `RemoteSigned` 并持久化到注册表，杜绝“禁止运行脚本”错误；
   - 自动递归对模块目录执行 `Unblock-File`，杜绝 PSReadLine“不可信发布者”安全提示；
7. **Windows Terminal 动态 Profile 探测与隐藏**：
   - 部署 Terminal 设置时，自动检测当前系统中是否真正已安装 NuShell 或 MSYS2；
   - 若未安装，动态在部署配置中标记 `"hidden": true`，避免用户误点报错 `0x80070002`；后续运行对应安装脚本时自动解除隐藏。

---


## ⏪ 一键回退指南 (`Restore-All.ps1` 与 `Restore-TerminalConfiguration.ps1`)

本项目采用**指针分离与救援快照机制**，在恢复前保留当前内容，并区分安装与部署快照：

### 1. 生产部署回退（针对 `Deploy-TerminalConfiguration.ps1`）
若仅需撤销生产部署同步（恢复 PowerShell 7 与 Windows Terminal 设置，可选 `-IncludeCmd`）：
```powershell
# 仅回退 PowerShell 7 与 Windows Terminal：
pwsh -File .\Restore-TerminalConfiguration.ps1

# 同时恢复 CMD AutoRun 与 Clink 配置：
pwsh -File .\Restore-TerminalConfiguration.ps1 -IncludeCmd
```
*(注：该脚本严格绑定 `.deployment-path` 快照指针，绝不会误回滚到更早的安装前状态。)*

### 2. 全终端系统级回退（针对 `Install-All.ps1` 或全局安装）
若需要将系统中所有终端配置恢复到安装前：
```powershell
# 1. 预览回退变更（-WhatIf 预演，不改变任何文件）：
pwsh -File .\Restore-All.ps1 -WhatIf
powershell.exe -File .\Restore-All.ps1 -WhatIf

# 2. 真正执行一键回退：
pwsh -File .\Restore-All.ps1
powershell.exe -File .\Restore-All.ps1
```

自定义 MSYS2 路径的新安装会同时更新仓库根目录的 `.last-install-context.json`，与 `.last-install-backup` 中的快照目录绑定。默认 `Restore-All.ps1` 只在两者匹配时使用该本机记录；不会从快照清单自动放宽目标白名单。

恢复历史快照、救援快照，或本机安装记录缺失时，显式提供当时使用的根目录：

```powershell
pwsh -NoProfile -File .\Restore-All.ps1 -BackupDirectory 'D:\backups\chosen-snapshot' -Msys2InstallPath 'D:\Tools\msys64' -WhatIf
# 核对后使用相同参数去掉 -WhatIf 执行恢复。
```

### 3. 回退机制核心保障
- **两阶段救援快照 (`pre-rollback-<guid>`)**：在执行任何覆写或清理前，先将当前目标文件及注册表备份至救援目录并记录 `restore-status.json`；
- **全终端 19 项资产覆盖**：覆盖 PS7、WinPS 5.1、Windows Terminal、CMD、NuShell（含 `zoxide.nu`）、MSYS2（含 `.bashrc`、4 类 `ini` 及 `MSYS2_PATH_TYPE` 注册表项）；
- **快照指针正交隔离**：部署快照（`.deployment-path`）、全局安装快照（`.last-install-backup`）与 CMD 卸载快照（`.last-cmd-backup`）独立解耦，彻底杜绝回退错选；
- **同卷临时文件原子替换**：`Set-TerminalBytes` 在目标同目录写临时文件并替换已有文件，避免直接截断目标。这是单文件操作，不是跨卷或多文件、注册表整体事务；
- **快照纯数据化安全**：快照仅作为只读数据与哈希凭证，严禁执行快照目录内的任何脚本，杜绝代码执行风险；
- **双 Shell 兼容**：在 `pwsh` 与系统原生 `powershell.exe`（支持 Win8.1/10/11）下均可一键执行。

---

## 🛠️ 执行策略受限时使用同一恢复入口

若普通本机会话的执行策略阻止运行已核实来源的仓库脚本，可只对本次进程指定执行策略，仍使用带哈希预检和救援快照的恢复入口。组织强制策略仍可能阻止执行，此时应由管理员按组织规则处理。

```powershell
# 部署回退预演，包含 CMD；仅需 PS7/Terminal 时省略 -IncludeCmd。
pwsh -NoProfile -ExecutionPolicy Bypass -File .\Restore-TerminalConfiguration.ps1 -IncludeCmd -WhatIf
# 核对后执行；发生错误时由恢复程序报告失败及救援目录。
pwsh -NoProfile -ExecutionPolicy Bypass -File .\Restore-TerminalConfiguration.ps1 -IncludeCmd
# 若撤销的是安装而非部署，使用 Restore-All.ps1。
```

不要直接删除 AutoRun 或按历史文件名复制备份。恢复若中途失败，部分目标可能已还原；请保留原快照及输出的救援目录，依据 `restore-status.json` 检查后再操作。救援副本保护的是捕获时的内容，不构成断电、磁盘故障或所有并发修改下的绝对无损保证。

---

## 💻 配合 IDE 与编辑器使用
部署完成后，如果您希望在 **VS Code** 或 **IntelliJ IDEA / PyCharm / WebStorm** 中同步获得无乱码的现代化美化终端，请查阅专门指南：
👉 **[IDE 与开发工具集成指南 (IDE与开发工具集成指南.md)](IDE与开发工具集成指南.md)**。
